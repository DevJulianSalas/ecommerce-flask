machine:

  timezone: America/Bogota
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker version
    - pip install --upgrade docker-compose


test:
  override:
    - docker-compose -f dev.yml run web_flask python apps/test_flask.py
