machine:
  pre:
    # actualiza docker para que docker-compose funcione correctamente
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0

  timezone: America/Bogota

  services:
    - docker

dependencies:
  override:
    - docker info
    - pip install --upgrade docker-compose


test:
  override:
    - docker-compose -f dev.yml run web_flask python apps/test_flask.py



deployment:
  hub:
    branch: develop
    commands:
      - docker-compose -f dev.yml build
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker-compose  -f dev.yml push
      # - bash -c "$DEPLOY"
      # - curl -H \"Content-Type: application/json\" --data '{\"source_type\": \"Branch\", \"source_name\": \"dev\"}' -X POST http://example.com

#"curl -H \"Content-Type: application/json\" --data '{\"source_type\": \"Branch\", \"source_name\": \"dev\"}' -X POST https://registry.hub.docker.com/u/devjulian/ecommerce-flask/trigger/e675ff17-2b32-4d39-b3a3-d4d43f0dc619/"

#"curl -H \"Content-Type:application/json\" --data '{\"build\": \"true\"}' -X POST https://registry.hub.docker.com/u/devjulian/ecommerce-flask/trigger/e675ff17-2b32-4d39-b3a3-d4d43f0dc619/"
